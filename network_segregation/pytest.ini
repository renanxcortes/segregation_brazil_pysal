[pytest]
testpaths = tests
addopts = -q
markers =
    realdata: needs the repository's census/shapefile inputs
    slow: long-running
filterwarnings =
    ignore::FutureWarning
    ignore::UserWarning
    ignore::RuntimeWarning
