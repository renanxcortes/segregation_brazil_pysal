[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
filterwarnings =
    ignore::RuntimeWarning
    ignore::UserWarning
    ignore::FutureWarning:segregation.singlegroup.spatial_dissim
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
