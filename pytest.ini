[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
filterwarnings =
    ignore::RuntimeWarning
    ignore::UserWarning
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
